# 🎯 الحل النهائي - ملف واحد فقط!

## ❌ المشكلة

**أنت تحاول رفع 3 ملفات مرة واحدة!**

الـ endpoint يتوقع **ملف واحد فقط** في كل request.

---

## ✅ الحل البسيط

### في Postman:

1. **احذف الـ 3 entries الموجودة** (سؤال 226 عام .jpeg3, سؤال 226 عام .jpeg4, سؤال 235 عام)

2. **أضف entry واحدة فقط:**
   - Key: `file`
   - Type: `File` ✅
   - Value: اختر **صورة واحدة فقط** (مثلاً: سؤال21عام.jpeg)

3. **Send**

4. **بعد النجاح، كرر للصورة التالية**

---

## 🚀 الحل الأسرع: استخدم السكريبت!

بدلاً من رفع 20 صورة يدوياً (20 مرة):

```powershell
# 1. احصل على Token
POST https://api.deutsch-tests.com/auth/login
# انسخ accessToken

# 2. في Terminal:
$env:JWT_TOKEN="your-access-token-here"
$env:API_BASE_URL="https://api.deutsch-tests.com"

# 3. شغل السكريبت
npm run upload-images-to-server
```

**السكريبت سيرفع جميع الـ 20 صورة تلقائياً!**

---

## 📋 القاعدة الذهبية

**ملف واحد = request واحد**

- ✅ صورة واحدة → Send → نجح
- ❌ 3 صور → Send → خطأ "Unexpected field"

---

## 🎯 جرب الآن

1. احذف الـ 3 entries
2. أضف entry واحدة فقط
3. Send
4. يجب أن ترى `201 Created` ✅

**بعدها استخدم السكريبت لرفع الباقي!**







